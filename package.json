{
  "name": "testxmd",
  "version": "0.0.0",
  "description": "Tests ex markdown! Use usage examples from a markdown file as unit tests",
  "main": "index.js",
  "scripts": {
    "prepublish" : "lsc --compile --bare index.ls",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anko <an@cyan.io>",
  "license": "ISC",
  "dependencies": {
    "mdast": "^0.19.0",
    "prelude-ls": "^1.1.1"
  },
  "devDependencies": {
    "LiveScript": "^1.3.1"
  }
}
